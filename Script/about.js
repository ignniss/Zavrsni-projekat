$(document).ready(() => {
    $('.btn').click(() => {

        let tekst = $('.text');
        let napisi = tekst[0].children('p');
        // $('#klik').text(tekst[2]);
        console.log(napisi);
    })

})